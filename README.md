# my neovim setup using kickstart.nvim
you might like [it](https://github.com/nvim-lua/kickstart.nvim/) too.
```git clone https://github.com/semim19/myNeovimConfig.git "${XDG_CONFIG_HOME:-$HOME/.config}"/nvim```
